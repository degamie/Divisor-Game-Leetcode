class Solution {
    public boolean divisorGame(int n) {
      //T(C(N)) S(C(N)) and  ==O(1) as it requires constant Time complexity in constant space
        //Optimized Solution
        return n%2==0;
    }
}
